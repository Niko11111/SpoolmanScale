#pragma once

// Backend selection screen. Reached from the Connection screen and shown
// instead of jumping straight into the address numpad, because a second
// backend brings credentials with it that need a place to live.
void buildBackendScreen();
