#pragma once

// ============================================================
//  SPOOLMAN OPTIONS
//
//  Settings that only mean anything in Spoolman mode, reached
//  from the filament manager screen. Same shape as the FilaMan
//  and BamBuddy options screens next to it, which is why the
//  extra fields moved in here: they were the one entry that
//  still hung directly off the backend screen.
// ============================================================

void buildSpoolmanOptionsScreen();
